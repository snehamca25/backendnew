# Node modules (do not upload dependencies)
node_modules/

# Environment variable files (never commit secrets like DB URIs or API keys)
.env

# Log files
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Operating system files
.DS_Store
Thumbs.db

# Build files (if you're using a build process)
dist/
build/

# VS Code settings (if sharing the repo, avoid editor-specific settings)
.vscode/

# Optional: ignore coverage output from testing
coverage/

# Optional: ignore compiled files if using TypeScript
*.tsbuildinfo

# Optional: if using Linux/Mac with SQLite (not your case, but safe to include)
*.sqlite

# Optional: for Heroku local dev (ignore local .env)
.env.local
.env.development.local
.env.test.local
.env.production.local